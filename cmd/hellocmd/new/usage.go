package new

const ProjectHelpTemplate = `
hellocmd [commands|flags]

The commands & flags are:
  new Create Hellocmd template project

  -d Build the specified directory for the template project

Examples:
  # Build the specified directory for the template project
  hellocmd new (your project name) -d (project dir)
  e.g.: hellocmd new helloapp -d .
`
